const { Given, When, Then } = require("@cucumber/cucumber");

Given('Close browser when all tests are finished', function () {
});

When('all tests are finished', function () {
});

Then('Close the browser', async function () {
});